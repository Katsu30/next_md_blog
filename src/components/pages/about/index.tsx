import { Background } from '@/components/features/app/Background';
import { MainLayout } from '@/components/features/app/Layout';

export const About = () => (
  <MainLayout className="lg:block" main={<Background />} />
);
